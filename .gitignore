.env
.env.*
.DS_Store
Thumbs.db
!.env.example
*.pem
*.key
service-account*.json
google-services.json
GoogleService-Info.plist
/coverage
/dist
docs/hikdocs/
/build
*.sqlite
# ETAPA 02 — monorepo
node_modules/
.turbo/
dist/
coverage/
*.tsbuildinfo

# CLI de Supabase
supabase/.temp/

# Medición de latencia del canal de tiempo real (ETAPA 06): se regenera en
# cada ejecución de la suite y su valor depende de la máquina.
.latencia-tiempo-real.json

# Next.js (ETAPA 09) — artefactos de compilación de la consola
.next/
next-env.d.ts
.vercel/
*.tsbuildinfo

# Claims que produce ./supabase/arranque-en-frio.sh; dependen de la base local.
.arranque-en-frio.json

# App Flutter: artefactos de compilacion y de analisis
/apps/mobile/build/
/apps/mobile/.dart_tool/
/apps/mobile/coverage/
/apps/mobile/.flutter-plugins-dependencies

# Basura que se coló en la raíz (ETAPA 12): un binario vacío de un `cc` suelto y
# el fichero de configuración de `openapi-generator`, que este proyecto NO usa
# (ADR-015: el cliente Dart se genera con `swagger_parser`). Los dos de 0 bytes.
a.out
openapitools.json

# Informes JSON del paso 5 del verificador (D-113): se borran al terminar.
.informe-paso5.json

# Informes JSON de las suites (instrumento de D-101, ETAPA 14): se conservan
# para poder subirlos como artefacto cuando el CI se pone rojo, y no se versionan.
/.informes-de-prueba/
docs/insumos/hikvision/evento-anpr-*.txt
